<UserControl x:Class="VR33B.LineGraphic.AxisMarkerLegendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VR33B.LineGraphic"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:VisibilityToCheckedConverter x:Key="VisibilityToCheckedConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Label x:Name="MarkerLabel" Content="{Binding Path=Description}"/>
            <Ellipse Width="15" Height="15" Stroke="{Binding Path=Stroke}" StrokeThickness="5"/>
            <Button x:Name="ColorPickerBtn" Width="20" Height="20" Margin="5,1,5,1">
                <Button.Content>
                    <Rectangle Stroke="Black" Fill="{Binding Path=Stroke}" Height="20" Width="20"/>
                </Button.Content>
            </Button>
            <CheckBox VerticalAlignment="Center" IsChecked="{Binding Path=Visibility, Converter={StaticResource VisibilityToCheckedConverter}, Mode=TwoWay}"/>
            <Slider VerticalAlignment="Center" Width="40" Minimum="1" Maximum="10" />

        </StackPanel>
    </Grid>
</UserControl>
